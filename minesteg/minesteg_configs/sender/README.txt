## Minesteg config files

This folder contains subfolders that represent a config for minesteg.
Each subfolder contains the config-file minesteg.properties.
Copy the chosen minesteg.properties into the minesteg.jar file.
Restart minesteg/minecraft to apply the changes.